import { CinderControlDirective } from './cinder-control.directive';

describe('CinderControlDirective', () => {
  it('should create an instance', () => {
    const directive = new CinderControlDirective();
    expect(directive).toBeTruthy();
  });
});
