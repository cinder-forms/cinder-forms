import { RangeInputDirectiveDirective } from './range-input-directive.directive';

describe('RangeInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RangeInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
