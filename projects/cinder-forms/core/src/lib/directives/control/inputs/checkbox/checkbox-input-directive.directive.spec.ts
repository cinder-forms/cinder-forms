import { CheckboxInputDirectiveDirective } from './checkbox-input-directive.directive';

describe('CheckboxInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
